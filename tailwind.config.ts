import type { Config } from "tailwindcss";

// Enable class-based dark mode so `dark:` variants react
// to the `.dark` class on the root element.
export default {
  darkMode: "class",
} satisfies Config;

